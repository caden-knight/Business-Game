// Keeping track of money
let bank = {
    wallet: 5000,
    bankAcct: 0,
    offshore: 0,
    businessEarnings: {
        counterfeit: 0,
        nightclub: 0,
        drugLord: 0,
        
    }
}

let items = {
    counterfeit: {
        printer: 2500
    }
}

let employees = {
    counterfeit: {
        production: {
            printOperater: 1,
        },
        export: {
            moneyRunner: 1,

        }
    }
}

let wallet = `$${bank.wallet}`
let bank = `$${bank}`
let offshore = `$${offshore}`